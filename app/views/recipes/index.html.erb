<%= render "shared/top_header"%> 
<div class="main_contaiter">

 <div>
  <h1>新着レシピ</h1>
 </div>



  
        <% @recipes.each do |recipe| %>
    <div class="flex__recipe"> 
      <div class="recipe__content">
        <div class="recipe_name"><%= recipe.title %></div>
          <div><%= link_to image_tag("#{recipe.image}", class:"recipe__image"), recipe, method: :get %></div>
          <div class="user_name"><%= recipe.user.nickname %></div> 
      </div>
    </div>

<% end %>
</div>
